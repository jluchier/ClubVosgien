mémo JJ about Yarn

Scripts de lancement => sh yarnRunWatch.sh
echo "je lance yarn run watch"
cd Developpements/ClubVosgien/
yarn run watch


installation de xampp

puis lien symbolique pour utiliser la même version de php partout

ln -s /opt/lampp/bin/php /usr/local/bin/php

pour lancer le service
sudo /opt/lampp/lampp start

Installation de npm

curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer
sudo chmod +x /usr/local/bin/composer
source ~/.bashrc (pour réinitialiser la console/le terminal)

si besoin avant installation de yarn
rm -rf node_modules
rm package-lock.json yarn.lock
npm cache clear --force

Installation de yarn
npm install --global yarn
puis
yarn add cross-env
yarn install

Si pb de version de npm
version manager de npm =>
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash
source ~/.bashrc

https://yarnpkg.com/en/docs/install#debian-stable

pour lancer le site en mode dev / consultation

Depuis le répertoire du site clubVosgien

utiliser la commande => yarn run watch

=> déploiement du site en production

	arrêter le moteur yarn run watch s'il fonctionne et fermer les fichiers ouverts du projet
	si besoin mettre à jour la version Dev du dépôt GitHub
 	compiler la version de production => yarn run prod
	Commit et push sur la branche Dev
	git checkout master
	git checkout master
	git merge Dev (Faire un merge de la branche Dev sur master)
	git push
	git checkout Dev (Revenir sur dev)
	
	=> (envoy run deploy (Envoyer la nouvelle version en production)) pas possible avec OVH (pas de connexion ssh)

	git reset --hard pour revenir au commit du depot


Temp package json dependencies  => "leaflet": "^1.7.1",

Application gpsmaster pour creer/modifier les fichiers gpx de Open Street Map
jj@jj-portable:~/Applications/gpsmaster$ java -jar GpsMaster_0.64.00-rc0.jar


Convertion des formats video WMV en GIF => j'ai utilisé le convertisseur en ligne https://convertio.co/fr/ qui fonctionne bien
